##Save Color Scheme in Linux Terminal

Export your profiles to a .dconf file.

'''bash
dconf dump /org/gnome/terminal/legacy/profiles:/ > ~/gnome-terminal-profiles.dconf

From the home file you can move it anywhere or use it on another computer.

Load with 

'''bash
$ dconf load /org/gnome/terminal/legacy/profiles:/ < /path/to/gnome-terminal-profiles.dconf

###Change Neofetch ASCII

Navigate to the neofetch config files

'''bash
~/.config/neofetch/config.conf

Edit the file
'''
vim ~/.config/neofetch/config.conf

Edit the image source section

'''python
image_source="/path/to/neofetch-ascii.txt"
